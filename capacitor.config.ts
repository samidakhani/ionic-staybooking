import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tenkmani.crrms',
  appName: 'airbnb',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
